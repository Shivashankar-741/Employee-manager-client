import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    flexGrow: 1,
  },
  AppBar: {
    backgroundColor: "#333",
  },
  title: {
    marginRight: "40px",
  },
  addEmployee: {
    cursor: "pointer",
  },
}));
